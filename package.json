{
  "name": "helpdesk-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix helpdesk",
    "build": "npm run build --prefix helpdesk",
    "preview": "npm run preview --prefix helpdesk",
    "lint": "npm run lint --prefix helpdesk"
  }
}
